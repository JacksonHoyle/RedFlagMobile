import React from 'react'
import AppContainer from './navigation/AppNavigator'
//I put AppNavigator in a folder within the main project directory called navigation so if you have a different file layout, make sure to change the location in the line of code directly above this comment

export default function App() {
	return <AppContainer />
}
